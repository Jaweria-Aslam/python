# Product factor N ki value
N = 3

# 1 se 10 tak multiplier ke liye loop
for multiplier in range(1, 11):

    # Multiplication result calculate karna
    result = N * multiplier

    # Formatted multiplication table print karna
    print(f"{N} x {multiplier} = {result}")